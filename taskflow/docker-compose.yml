version: "3.8"
services:
  taskflow:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - taskflow-data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/taskflow.db
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
volumes:
  taskflow-data:
